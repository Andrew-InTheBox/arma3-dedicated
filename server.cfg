// Arma 3 Dedicated Server Configuration
// Reference: https://community.bistudio.com/wiki/server.cfg

hostname = "CHANGE_ME - Antistasi Ultimate";
password = "";
passwordAdmin = "CHANGE_ME_ADMIN";

maxPlayers = 20;

// Keep mission running when server is empty (required for Antistasi persistence)
persistent = 1;

// Mission voting
voteMissionPlayers = 1;
voteThreshold = 0.33;

// Voice
disableVoN = 0;
vonCodecQuality = 3;

// Security — set verifySignatures = 2 and BattlEye = 1 on public server
BattlEye = 0;
verifySignatures = 0;
allowedFilePatching = 1;

// Logging
logFile = "config\server_console.log";
timeStampFormat = "short";

// Antistasi requires file writing for its save system
// allowedVoteCmds[] = {};

class Missions
{
    // Uncomment and set the correct mission template name after first run.
    // The mission .pbo names are inside @AntistasiUltimate\mpmissions\.
    // Example: AntistasiUltimate_1.Altis (filename without .pbo extension)
    //
    // class Mission
    // {
    //     template = "AntistasiUltimate_1.Altis";
    //     difficulty = "Regular";
    //     params {};
    // };
};
